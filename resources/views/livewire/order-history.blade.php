<div class="mx-auto max-w-5xl px-4 py-8"><div class="flex flex-wrap items-end justify-between gap-4"><div><h1 class="text-3xl font-black">Pesanan Saya</h1><p class="text-slate-500">Pantau status pesanan terbaru.</p></div><select wire:model.live="status" class="input w-44"><option value="">Semua status</option><option value="pending">Pending</option><option value="diproses">Diproses</option><option value="selesai">Selesai</option></select></div>
    <div class="mt-6 space-y-3">@forelse($orders as $order)<a href="{{route('orders.show',$order)}}" wire:navigate class="panel flex flex-wrap items-center gap-4 p-5 transition hover:border-red-200"><div class="flex-1"><div class="flex items-center gap-2"><b>{{$order->kode_pesanan}}</b><span class="status status-{{$order->status->value}}">{{$order->status->label()}}</span></div><p class="mt-1 text-sm text-slate-500">{{$order->created_at->translatedFormat('d M Y, H:i')}} · {{$order->kelas->nama}}</p></div><b class="text-red-600">Rp{{number_format($order->total,0,',','.')}}</b><span>→</span></a>@empty<div class="panel p-12 text-center text-slate-500">Belum ada pesanan.</div>@endforelse</div><div class="mt-6">{{$orders->links()}}</div>
</div>
